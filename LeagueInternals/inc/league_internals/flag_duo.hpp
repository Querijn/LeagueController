#pragma once

#include <league_controller/types.hpp>

namespace LeagueController
{
	struct FlagDuo
	{
		// This is a redacted structure that was used to store encrypted data.
		// The fields below are not correct, and are only placeholders.
		u8 flag1;
		u8 flag2;
	};
}